apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-service
  namespace: smart-city
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-service
  template:
    metadata:
      labels:
        app: api-service
    spec:
      serviceAccountName: smart-city-sa
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: api-service
          image: ghcr.io/example/smart-city/api-service:latest
          ports:
            - containerPort: 8004
          envFrom:
            - configMapRef:
                name: smart-city-config
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          livenessProbe:
            httpGet:
              path: /health
              port: 8004
            initialDelaySeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 8004
            initialDelaySeconds: 8
---
apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: smart-city
spec:
  type: ClusterIP
  selector:
    app: api-service
  ports:
    - port: 8004
      targetPort: 8004
