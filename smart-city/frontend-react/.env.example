# Public base URL of the API service (no trailing slash).
# Production: HTTPS URL of your API (AKS ingress / LB), e.g. https://api.example.com
# Local: leave unset to use http://localhost:8004
VITE_SMART_CITY_API_BASE=
